% main.tex — manuscript skeleton (venue-agnostic).
% Swap \documentclass for your venue style:
%   CVPR/ICCV : \documentclass[10pt,twocolumn,letterpaper]{article} + cvpr.sty
%   NeurIPS   : \documentclass{article} + neurips_2024.sty
%   ICLR      : iclr2025_conference.sty
%   Journal   : IEEEtran / elsarticle
% This default builds standalone with pdflatex + bibtex.
\documentclass[10pt,twocolumn,letterpaper]{article}

\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}      % results tables (see latex-results-table skill)
\usepackage{amsmath,amssymb}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
% cleveref is nicer but not in minimal TeXLive; shim so \cref/\Cref compile as \ref.
% For prettier output: tlmgr install cleveref, then delete these two lines + uncomment below.
\providecommand{\cref}[1]{\ref{#1}}
\providecommand{\Cref}[1]{\ref{#1}}
% \usepackage{cleveref}

\graphicspath{{figures/}}

\title{OVD-Diagnose: Decomposing Open-Vocabulary Detector Failure\\ Across Specialized Domains}
\author{
  Shazzad Hossain Mazumder\thanks{Corresponding author:
  \texttt{shmazumder@feniuniversity.ac.bd}} \quad Kazi Nazmul Hasan \\
  Muntasir Karim Chowdhury \quad Shahriar Zaman Abid \\[2pt]
  Department of Computer Science \& Engineering, Feni University
}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Open-vocabulary object detectors (OVD) generalize poorly outside natural images, but
\emph{why} they fail in specialized domains is not measured: aggregate metrics such as
mAP collapse several distinct error sources into one number. We introduce
\textbf{OVD-Diagnose}, a diagnostic protocol and cross-domain benchmark that decomposes
OVD failure into three interpretable axes --- localization, semantic confusion, and
calibration --- using controlled inference modes (global vocabulary, oracle vocabulary,
and a class-agnostic proposer). Applied to frozen detectors across specialized domains,
the protocol indicates that the dominant failure axis differs across the two domains we
study: on aerial imagery detectors retain measurable localizability and oracle prompting
substantially raises the (still-low) AP, whereas on chest radiographs localizability is near
zero under \emph{both} an external proposer and the detectors' own boxes, so vocabulary
disambiguation cannot help. We report these as descriptive observations, not domain-level
laws, and stress that all detectors operate in a low-AP regime. The protocol is
training-free, model-agnostic, and reproducible on modest hardware.
\end{abstract}

\section{Introduction}
\label{sec:intro}
Open-vocabulary object detection (OVD)~\cite{radford2021clip} promises detection of
arbitrary categories from text, yet its zero-shot performance degrades sharply outside
the natural-image distribution it was trained on. This matters precisely where OVD would
be most useful: aerial survey, medical screening, and other specialized domains where
exhaustively annotating every category is impractical. Recent benchmarks confirm the
degradation but report it as a single collapsed number (mAP or F1), which cannot say
\emph{why} a detector fails --- whether it cannot find the objects, cannot name them, or
simply mis-thresholds its own confidence. These are different problems demanding different
remedies, and conflating them has slowed progress on specialized-domain OVD.

We argue that OVD failure should be \emph{decomposed}, not summarized. Closed-set error
toolkits (e.g.\ TIDE) dissect detector errors but predate and ignore the axis unique to
OVD: error induced by the open vocabulary itself. We introduce \textbf{OVD-Diagnose}, a
training-free protocol that isolates three axes with controlled inference modes ---
prompting a detector with the full domain vocabulary, with only the classes present in an
image, and (for localization) with a class-agnostic proposer --- and reads off
localization ($L$), semantic-confusion ($S$), and calibration ($C$) error from the gaps
between them.

Our contributions are:
\begin{itemize}
  \item \textbf{A diagnostic protocol} that decomposes OVD failure into localization,
  semantic-confusion, and calibration axes, with a construct-validation design (synthetic
  controls) and an IoA-F1 anchor that reproduces prior benchmarks' model ordering. To reduce
  dependence on any single proposer, localization is measured both by an external
  segmenter and by the detector's own class-agnostic boxes.
  \item \textbf{A diagnostic study} across frozen OVD detectors and two specialized domains,
  released as an open toolkit for arbitrary detector/domain pairs.
  \item \textbf{Observations:} on the datasets studied, the dominant residual failure axis
  differs (aerial retains localizability with a large vocabulary cost; medical is
  localization-limited under multiple proposers) --- distinctions invisible to aggregate mAP.
  We present these as hypotheses for larger-scale study, not domain-level conclusions.
\end{itemize}

\section{Related Work}
\label{sec:related}
\textbf{Open-vocabulary detection.} Vision-language pre-training~\cite{radford2021clip}
enabled detectors that localize categories specified by text at inference. Grounding-based
and region-text methods --- GLIP, Grounding DINO~\cite{liu2023groundingdino},
YOLO-World~\cite{cheng2024yoloworld}, and dense image-text matching in
OWLv2~\cite{minderer2023scaling} --- achieve strong zero-shot results on natural images.
Their performance outside that distribution, however, is far weaker.

\textbf{OVD in specialized domains.} Recent studies document sharp degradation of
general-purpose OVD on aerial imagery and propose adaptation via few-shot
refinement~\cite{refael2025flame} or domain-specific pre-training. These works establish
\emph{that} OVD transfers poorly; we ask \emph{which} failure mode is responsible, and show
the answer differs by domain.

\textbf{Error diagnosis and calibration.} TIDE~\cite{bolya2020tide} decomposes closed-set
detector errors (duplicate, background, localization, classification), but its taxonomy
predates and cannot express the axis induced by an open vocabulary. Concurrent training-free
efforts adapt or calibrate OVD at inference~\cite{procal2026,factor2026}; these are point
remedies, whereas we provide a domain-agnostic \emph{diagnosis} that indicates which remedy a
domain even needs. A class-agnostic segmenter~\cite{kirillov2023sam} supplies our
localization reference.

\section{The OVD-Diagnose Protocol}
\label{sec:method}
\textbf{Setup.} Let $M$ be a frozen OVD detector, $x$ an image, $V$ the full domain
vocabulary ($|V|{=}N$), and $P(x)\subseteq V$ the categories actually present in $x$
(from ground truth). $M(x,T)$ runs detection prompted with class set $T$. We evaluate three
inference modes:
\begin{itemize}
  \item \textbf{Global} ($T{=}V$): the deployment setting; localization, confusion, and
  calibration act jointly.
  \item \textbf{Oracle} ($T{=}P(x)$): removes vocabulary confusion by prompting only
  present classes.
  \item \textbf{Agnostic}: a class-agnostic proposer~\cite{kirillov2023sam} bounds pure
  localizability, independent of any text.
\end{itemize}

\textbf{Failure axes.} From these modes we read three interpretable errors.
\emph{Localization} uses class-agnostic recall $\mathrm{AR}_{\mathrm{SAM}}$:
\begin{equation}
  L = 1 - \mathrm{AR}_{\mathrm{SAM}},
  \label{eq:L}
\end{equation}
so $L{\to}1$ means objects cannot be boxed at all, regardless of naming. To avoid dependence
on a single proposer, we also report a detector-intrinsic variant $L_{\mathrm{det}}$ that
strips labels from the detector's own oracle-mode boxes; agreement between $L$ and
$L_{\mathrm{det}}$ indicates the localization reading is not an artifact of the external
proposer.
\emph{Semantic confusion} is the AP lost purely to enlarging the vocabulary from $P(x)$ to
$V$:
\begin{equation}
  \begin{aligned}
    S &= \mathrm{AP}_{\mathrm{oracle}} - \mathrm{AP}_{\mathrm{global}}, \\
    S_{\mathrm{norm}} &= \frac{S}{\max(\mathrm{AP}_{\mathrm{oracle}},\,\varepsilon)}.
  \end{aligned}
  \label{eq:S}
\end{equation}
$S_{\mathrm{norm}}$ is only meaningful when $\mathrm{AP}_{\mathrm{oracle}}$ is non-trivial;
we flag it otherwise. \emph{Calibration} combines the expected calibration error of detection
confidences with an operating-point term,
\begin{equation}
  \begin{aligned}
    C_{\mathrm{ece}} &= \sum_b \tfrac{|B_b|}{D}\,
      \bigl|\mathrm{acc}(B_b)-\mathrm{conf}(B_b)\bigr|, \\
    C_{\mathrm{thr}} &= F_1^{*} - F_1^{\mathrm{fixed}},
  \end{aligned}
  \label{eq:C}
\end{equation}
where $B_b$ are confidence bins and $F_1^{*}$ the best achievable $F_1$ over thresholds.
Each $(\text{domain},\text{model})$ thus yields a fingerprint $(L, S, C)$.

\textbf{Validation.} To establish construct validity, the protocol prescribes perturbations
with known effect: injecting near-synonym distractor classes into $V$ should raise $S$ while
leaving $L,C$ fixed; blurring inputs should raise $L$; temperature-scaling logits should move
$C$. Empirically, we anchor absolute performance with an IoA-F1 score matching prior aerial
benchmarks, confirming the harness reproduces their model ordering.

\section{Experiments}
\label{sec:exp}
\subsection{Setup}
\textbf{Domains.} We evaluate two specialized domains with multi-class box annotations:
\emph{aerial}, using the LAE-80C benchmark~\cite{pan2024lae} (80 categories aggregated from
DOTA, DIOR, FAIR1M, and xView), and \emph{medical}, using VinDr chest
radiographs~\cite{nguyen2022vindr} (14 thoracic finding classes). Both are re-expressed in a
common COCO annotation format. \textbf{Models.} We diagnose three frozen detectors spanning
the dominant OVD paradigms --- YOLO-World~\cite{cheng2024yoloworld} (real-time),
OWLv2~\cite{minderer2023scaling} (dense image-text matching), and Grounding
DINO~\cite{liu2023groundingdino} (grounding) --- all evaluated zero-shot with no
domain-specific fine-tuning. \textbf{Metrics.} We report COCO mAP@[.5:.95] under global and
oracle prompting, the $(L,S,C)$ fingerprint of Eqs.~\eqref{eq:L}--\eqref{eq:C}, and an
IoA-F1 anchor. Localization uses a frozen SAM proposer~\cite{kirillov2023sam} in
``everything'' mode. \textbf{Hardware.} All experiments are inference-only and run on a
single consumer GPU, underscoring the protocol's accessibility.

\subsection{Main Results}
\input{tables/ovd_fingerprint}
\Cref{tab:fingerprint} reports the failure fingerprint of frozen open-vocabulary detectors
across the two specialized domains, and \Cref{fig:fingerprint} summarizes the dominant axis
per domain.
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fingerprint}
  \caption{Cross-domain failure profile (mean over models). Aerial retains measurable
  localizability ($\mathrm{AR}_{\mathrm{SAM}}$) with a large vocabulary cost; medical
  collapses on localization, where $S$ is undefined (hatched).}
  \label{fig:fingerprint}
\end{figure}

\subsection{Cross-domain findings}
\label{sec:findings}
We frame these as descriptive observations on two datasets, not domain-level laws, and note
throughout that absolute AP is low ($\le 0.08$ even under oracle prompting), so the
decomposition characterizes the \emph{shape} of a failure regime rather than a
near-solved task.

\textbf{(1) The residual bottleneck differs by domain.} On aerial imagery, detectors retain
some localizability ($\mathrm{AR}_{\mathrm{SAM}}{=}0.138$, and the detector-intrinsic
$\mathrm{AR}_{\mathrm{det}}$ agrees to within $0.10$ for OWLv2 and Grounding DINO), and
oracle prompting raises the (still-low) AP by ${\approx}5{\times}$ for OWLv2
($0.015{\to}0.079$), so a large share of the residual error is attributable to vocabulary.
On chest radiographs, oracle prompting yields no usable gain
($0.0002{\to}0.0012$, both ${\approx}0$) and localizability is low under every reference we
tried --- localization, not vocabulary, is the limiting factor.

Here the two localization estimates \emph{diverge}, and the direction matters. SAM proposes
almost nothing on radiographs ($\mathrm{AR}_{\mathrm{SAM}}{=}0.0024$), whereas the detectors'
own class-agnostic boxes recover $23{\times}$ more ground truth
($\mathrm{AR}_{\mathrm{det}}{=}0.057$ for OWLv2). A natural-image proposer is therefore
demonstrably unreliable in this domain, and the SAM-based $L$ overstates the severity of the
localization gap. The qualitative conclusion nevertheless survives the correction: even the
proposer-independent estimate leaves $L_{\mathrm{det}}{=}0.94$, i.e.\ $94\%$ of findings
unlocalized before any label is assigned. We accordingly report $L_{\mathrm{det}}$ as the
primary localization figure on medical and treat the SAM-based $L$ as a lower bound on
localizability there. A domain-trained proposer would tighten this estimate further and is
the check we most want (Sec.~\ref{sec:limitations}).

\Cref{fig:qual} shows representative cases: on aerial imagery detections often localize a
GT object but assign a wrong (semantically adjacent) class, whereas on chest radiographs GT
findings are frequently left with no overlapping detection at all.
\begin{figure}[t]
  \centering
  \includegraphics[width=0.49\linewidth]{figures/qual_aerial}
  \includegraphics[width=0.49\linewidth]{figures/qual_medical}
  \caption{GT (green) vs.\ detections (red). Left (aerial): boxes land on objects but with
  wrong class labels --- semantic confusion. Right (medical): findings go undetected ---
  localization failure.}
  \label{fig:qual}
\end{figure}

\textbf{(2) A capability trend (hypothesis).} $S_{\mathrm{norm}}$ is numerically larger for
higher-capacity detectors in our aerial results, but because it is a ratio on small
$\mathrm{AP}_{\mathrm{oracle}}$ values and rests on three models, we report it only as a
hypothesis for larger-scale study, not a conclusion. Regardless, these vocabulary-induced
distinctions are invisible to closed-set error tools (e.g.\ TIDE).

\textbf{(3) Calibration is model-, not domain-, driven.} The reliability diagrams of
\Cref{fig:reliability} show detections lie below the diagonal (overconfident), with Grounding
DINO markedly worse ($C_{\mathrm{ece}}{=}0.084$) than YOLO-World ($0.009$). Unlike $L$ and
$S$, the calibration axis separates \emph{models} rather than domains, indicating it captures
a distinct, complementary failure mode: a detector may be well-calibrated yet localization- or
vocabulary-bound, or vice versa.
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/reliability_aerial}
  \caption{Reliability diagrams (aerial). Empirical precision vs.\ mean confidence per model;
  curves below the diagonal indicate overconfidence. ECE in legend.}
  \label{fig:reliability}
\end{figure}

\subsection{Reproducibility}
\label{sec:repro}
We report the full protocol so results can be reproduced exactly. Detection quality is COCO
mAP@[.50:.95] computed with \texttt{pycocotools}, restricted to the images actually evaluated
(a subset run reports its own denominator). The anchor metric is IoA-F1 at IoA threshold
$0.7$ and score threshold $0.25$, matching prior aerial benchmarks. Inference uses a low score
threshold ($0.001$) so calibration observes the full confidence range; the fixed operating
point for $C_{\mathrm{thr}}$ is $0.25$. Prompts are the benchmark's raw category names
(Grounding DINO uses the lowercase period-separated convention). The default proposer is
Ultralytics SAM (\texttt{mobile\_sam}, ``everything'' mode, $1024$px); YOLO-World runs at
$800$px, OWLv2 and Grounding DINO at their processor defaults. Categories are mapped to a
contiguous space internally and mapped back to dataset ids before scoring. Images that cannot
be read are skipped and excluded from the denominator. Code, adapters, and the exact run
scripts are released with the benchmark; uncertainty is estimated by image-level bootstrap
($1000$ resamples).

\subsection{Validation}
\label{sec:validation}
The IoA-F1 anchor reproduces the model ordering of prior aerial benchmarks (OWLv2 best),
confirming the harness against external results. Bootstrap CIs on the anchor
(\Cref{tab:fingerprint}) do not overlap across the three detectors in either domain, so the
reported ordering is not attributable to sampling noise.

We then run the synthetic controls of Sec.~\ref{sec:method}, each perturbing one factor and
checking that only its intended axis moves.
\textbf{Temperature.} Rescaling logits over $T{\in}[0.5,5]$ drives $C_{\mathrm{ece}}$ from
$0.003$ to $0.287$ on aerial ($0.001{\to}0.261$ on medical) while AP is invariant to four
decimal places ($0.0150$ at every $T$; $0.0002$ on medical). The calibration axis is thus
cleanly separable from the AP-derived axes --- the sharpest of our validity results, and it
replicates in both domains.
\textbf{Vocabulary.} Adding random distractors to the prompt raises $S$ monotonically on
aerial ($0.000, 0.037, 0.070, 0.103, 0.125$ at $0, 5, 20, 40, 79$ distractors), confirming
$S$ tracks vocabulary pressure.
\textbf{Blur.} Gaussian blur at $\sigma{\in}[0,8]$ raises $L$ monotonically on aerial
($0.611{\to}0.796$) via a text-independent perturbation, confirming $L$ responds to image
degradation rather than prompting. The same control on medical is uninformative: $L$ begins
at $0.996$ and can move at most $0.004$, a floor effect we report as inconclusive rather
than as a pass.

Two gaps remain. We do not yet report the semantically-ranked variant of the vocabulary
control, which would distinguish ``$S$ grows with vocabulary \emph{size}'' from ``$S$ grows
with vocabulary \emph{confusability}''; only the former is presently established. And the
controls are demonstrated on aerial, where the metrics are non-degenerate; medical supports
only the temperature control for the reason given above.

\section{Limitations}
\label{sec:limitations}
Our study is deliberately scoped to what can be measured rigorously on modest hardware, and
several limitations follow.

\textbf{The medical domain is a degenerate regime.} This is the most consequential caveat.
On VinDr-CXR, AP is ${\approx}0$ under \emph{every} prompting mode --- global, oracle, and
agnostic alike. When all quantities collapse together, the decomposition cannot attribute
error \emph{between} axes; it can only report that localization and semantics fail jointly.
The discriminative value of the fingerprint --- its ability to say ``this domain is
vocabulary-bound, that one is not'' --- is therefore demonstrated on aerial imagery, while
medical functions as a contrasting endpoint rather than as a second case of successful
decomposition. A reader should not conclude that we have separated three axes in two
domains; we have separated them in one and shown a second where the separation degenerates.
Establishing the protocol's discriminative power properly requires domains of intermediate
difficulty, where AP is low but non-vanishing.

\textbf{Localization reference.} A single natural-image SAM proposer conflates
``intrinsically hard to localize'' with ``hard for SAM,'' and our medical results show this
is not hypothetical: SAM recovers $0.24\%$ of findings while the detectors' own boxes
recover $5.7\%$, a $23{\times}$ gap. We therefore report the proposer-independent
$L_{\mathrm{det}}$ as primary in that domain and treat the SAM-based $L$ as a lower bound.
The strongest available check --- a chest-radiograph-trained proposer plugged in through the
same interface --- is implemented in the released toolkit but not yet run for want of a
suitable public weight; until it is, the \emph{magnitude} of the medical localization gap
should be read as bounded rather than pinned down.

\textbf{The $S$ axis.} $S_{\mathrm{norm}}$ is a ratio on $\mathrm{AP}_{\mathrm{oracle}}$ and
is unstable when localization collapses; the medical values are reported for completeness
but are not interpreted. It supports a \emph{within-aerial} reading only. Moreover, our
vocabulary control establishes that $S$ responds to vocabulary size, but not yet that it
responds specifically to semantic confusability (Sec.~\ref{sec:validation}); the axis's
name is thus, at present, better justified by the qualitative evidence
(\Cref{fig:qual}) than by the control.

\textbf{Scale.} Two domains, three detectors. The aerial split is evaluated on the $1325$
images retrievable at run time of $3592$ annotated, so aerial numbers are subset estimates;
medical uses the full $4394$. Absolute values will shift with complete splits and additional
detectors, and two datasets cannot support claims about ``aerial'' or ``medical'' imagery in
general --- we describe two benchmarks, not two domains.

\textbf{Vocabulary construction.} We use each benchmark's native taxonomy; alternative
groupings or prompt templates would change $S$. Radiological class names
(e.g.\ ``ILD'', ``Pulmonary fibrosis'') are also far from the caption distributions these
detectors were trained on, so the medical result partly measures text-encoder coverage
rather than detection capability per se --- a confound we cannot separate without
prompt-engineering ablations we leave to future work.

\section{Conclusion}
\label{sec:conc}
Aggregate mAP tells us that open-vocabulary detectors fail in specialized domains but not
why. OVD-Diagnose replaces that single number with an interpretable, training-free
fingerprint that separates localization, semantic-confusion, and calibration error, and it
reveals that the dominant failure mode is domain-specific: on aerial imagery a large share
of the residual error is attributable to vocabulary confusion, whereas on chest radiographs
localization fails first and vocabulary never becomes the binding constraint. We show the
decomposition doing real work in the first setting and degenerating in the second, and we
report both. Because the protocol is model-agnostic and runs inference-only, it
scales to new detectors and domains at negligible cost and turns ``it doesn't transfer''
into an actionable diagnosis of which remedy --- better localization, vocabulary
disambiguation, or recalibration --- a given deployment actually needs. We release the
benchmark and toolkit to support this diagnosis across the community.

{\small
\bibliographystyle{plain}   % built-in; swap to ieee_fullname/abbrvnat per venue
\bibliography{refs}
}

\end{document}
